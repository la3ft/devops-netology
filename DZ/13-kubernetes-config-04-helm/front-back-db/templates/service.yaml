apiVersion: v1
kind: Service
metadata:
  name: {{ include "front-back-db.fullname" . }}
  labels:
    {{- include "front-back-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "front-back-db.selectorLabels" . | nindent 4 }}
